# SQLite version 3.x
#   gem install sqlite3-ruby (not necessary on OS X Leopard)
development:
    adapter: postgresql
    host: localhost
    port: 5432
    username: integrity
    password: stag0storm
    database: integrity_vote_reports_development
    encoding: utf8

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test: &TEST
    adapter: postgresql
    host: localhost
    port: 5432
    username: integrity
    password: stag0storm
    database: integrity_vote_reports_test
    encoding: utf8

production: &PRODUCTION
    adapter: postgresql
    host: localhost
    port: 5432
    username: integrity
    password: stag0storm
    database: integrity_vote_reports_production
    encoding: utf8

cucumber:
  <<: *TEST

staging:
  <<: *PRODUCTION
