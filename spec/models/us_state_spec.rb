require 'spec_helper'

describe UsState do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    UsState.create!(@valid_attributes)
  end
end
