require 'spec_helper'

describe ReportCriterion do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    ReportCriterion.create!(@valid_attributes)
  end
end
