<% title 'Politicians' %>

<div class="section">
  <%= render 'politicians/search', :url => politicians_path, :replace => '#politicians' %>

  <%= render 'politicians/list', :politicians => @politicians %>
</div>