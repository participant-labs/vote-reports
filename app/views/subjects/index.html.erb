<% title "Subjects" %>

<div class="section">
  <h2>Find subjects you care about</h2>

  <%= render 'subjects/search', :replace => 'subjects', :q => params[:q] %>
</div>

<div class="section">
  <%- if params[:q] -%>
    <%= render 'subjects/list', :subjects => @subjects, :title => @title %>
  <%- else -%>
    <div id="subjects">
      <h3><%= @title %></h3>
      <%= render 'subjects/cloud', :subjects => @subjects %>
    </div>
  <%- end -%>
</div>
