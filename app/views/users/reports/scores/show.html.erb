<%- title score_title(@score) -%>
<div>
  <%= render 'reports/scores/evidence', :score => @score %>
  <dl>
    <%- @score.evidence.group_by(&:subject).each_pair do |subject, scores| -%>
      <%= render 'reports/scores/evidences/evidence', :subject => subject, :scores => scores %>
    <%- end -%>
  </dl>
</div>
