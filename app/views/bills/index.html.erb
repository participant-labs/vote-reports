<% title params[:q].present? ? %Q{Bill Search Results for "#{h params[:q]}"} : 'Bills' %>

<div class="section">
  <h2>Bill Search</h2>

  <% form_tag bills_path, :method => :get do %>
    <%= label_tag :q, 'Search' %>
    <%= text_field_tag :q, params[:q] %>
    <%= submit_tag 'Search' %>
  <% end %>

  <h3><%= @title %></h3>
  <%= render 'bills/list', :bills => @bills %>
</div>
