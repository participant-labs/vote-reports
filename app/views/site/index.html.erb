<%- title 'Knowing is Half the Ballot' -%>
<div class="section clearfix">
  <h3>Know what your representatives <em>really</em> stand for</h3>
  <p>We want you to be empowered to make good choices when you vote, so we make it easy to know how your representatives vote on the issues important to you.  Just tell us your zip code, tell us what you care about, and we'll tell you how they're doing.</p>

  <h4>Try it out</h4>
  <div class="clearfix">
    <p class="left">On the left are various interest groups.  On the right is a score from each group, and the representative who received it.</p>
    <div class="right">
      <div class="guide_entry">
        <% form_tag root_path, :method => :get, :class => 'tag_cloud', :'data-replace' => 'score_examples' do %>
          <dl>
            <dt><%= label_tag :representing, 'Next, Enter your zip code to see just your Reps' %></dt>
            <dd><%= text_field_tag :representing, requested_location, :tabindex => 1, :size => 10 %></dd>
          </dl>
          <p><%= submit_tag 'Know More' %></p>
        <% end %>
      </div>
    </div>
  </div>

  <div id="score_examples">
    <%= render 'scores/list', :scores => @scores %>
  </div>
</div>

<table class="section" id="featured_recent_reports">
  <thead>
    <tr>
      <th scope="col">Recent User Reports</th>
      <th scope="col">Featured Interest Group Reports</th>
    </tr>
  </thead>
  <tbody>
    <%- @recent_reports.zip(@featured_interest_group_reports) do |recent, featured_ig| -%>
      <tr>
        <td><%= render 'reports/report', :report => recent if recent %></td>
        <td><%= render 'reports/report', :report => featured_ig if featured_ig %></td>
      </tr>
    <%- end -%>
    <tr>
      <td><%= link_to "See More...", reports_path %></td>
      <td><%= link_to "See More...", interest_groups_path %></td>
    </tr>
  </tbody>
</table>
