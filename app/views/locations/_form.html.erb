<%-
  no_ajax ||= false
  replace ||= @js ? 'fancybox-inner' : 'location_setter'
  format ||= @js ? 'js' : 'html'
-%>

<% form_tag location_path(:format => format), no_ajax ? {} : {:class => "act-replace", :rel => replace}  do %>
  <%= label_tag :location, 'Your Location', :style => 'font-weight: normal;' %>
  <%= text_field_tag :location, session[:declared_location] %>
  <%- if params[:return_to].present? -%>
    <%= hidden_field_tag :return_to, params[:return_to] %>
  <%- end -%>
  <%= submit_tag 'Set' %>
<% end %>
